export type AgentState = "idle" | "searching" | "analyzing" | "matching" | "preparing" | "review_required" | "ready" | "submitted" | "error";
export function AgentStatus({ state, title, detail }: { state: AgentState; title: string; detail: string }) { return <section className={`agent-status state-${state}`} aria-live="polite"><span className="agent-orb" aria-hidden="true" /><div><p className="eyebrow">Agente activo</p><strong>{title}</strong><p>{detail}</p></div></section>; }
export function MetricCard({ label, value, detail }: { label: string; value: number | string; detail: string }) { return <article className="metric-card"><span>{label}</span><strong>{value}</strong><small>{detail}</small></article>; }
export function ActivityFeed({ items }: { items: Array<{ state: AgentState; text: string }> }) { return <section className="activity-feed" aria-label="Actividad reciente"><h2>Actividad reciente</h2>{items.map((item, index) => <p key={index} className={`activity-${item.state}`}><span aria-hidden="true" />{item.text}</p>)}</section>; }
